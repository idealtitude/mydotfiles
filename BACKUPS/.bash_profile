# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

# User specific environment and startup programs
# function starting_stef_sess {
    # sleep 10
    # picom --backend glx &
    # xset s off -dpms
# }
#
# starting_stef_sess &

# eval "$(ssh-agent -s)"
# ssh-add -k ~/.ssh/id_ed25519
